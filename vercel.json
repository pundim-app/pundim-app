{
  "version": 2,
  "buildCommand": "pnpm build && npx expo export --platform web --output-dir dist/web",
  "installCommand": "pnpm install",
  "framework": null,
  "public": true,
  "env": {
    "NODE_ENV": "production"
  },
  "outputDirectory": "dist/web"
}
